<!-- app/views/wallets/_form.html.erb -->

<%= form_with(model: @wallet, local: true) do |form| %>
  <% if @wallet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wallet.errors.count, 'error') %> prohibited this wallet from being saved:</h2>

      <ul>
        <% @wallet.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.hidden_field :user_id ,value:current_user.id%>
  </div>

  <div class="field">
    <%= form.label :balance %>
    <%= form.number_field :balance %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<!-- app/views/wallets/_form.html.erb -->

<%= form_with(model: @wallet, local: true) do |form| %>
  <% if @wallet.errors.any? %>
    <div class="alert alert-danger">
      <h4><i class="icon fas fa-ban"></i> <%= pluralize(@wallet.errors.count, 'error') %> prohibited this wallet from being saved:</h4>
      <ul>
        <% @wallet.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="card-body">
    <div class="form-group">
      <%= form.hidden_field :user_id, value: current_user.id, class: 'form-control' %>
    </div>

    <div class="form-group">
      <%= form.label :balance %>
      <%= form.number_field :balance, class: 'form-control' %>
    </div>
  </div>

  <div class="card-footer">
    <%= form.submit 'Save', class: 'btn btn-primary' %>
  </div>
<% end %>
